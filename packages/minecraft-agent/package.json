{
  "name": "@moneyverse/minecraft-agent",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "Loopback-only, allowlisted Minecraft systemd control agent",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.19.9",
    "typescript": "^5.9.0",
    "vitest": "^3.0.0"
  },
  "main": "dist/server.js"
}
