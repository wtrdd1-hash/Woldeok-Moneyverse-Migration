# QA 보강 — v2026.09.14.78

날짜: 2026-09-14
브랜치: `fix/qa-hardening-v2026.09.14.78`
상태: 구현 / 검증

## 범위

반복 QA에서 발견된 복구 스크립트 의존성 공백을 보완합니다. `deploy/restore.sh`는 기본 복구 DB 이름 생성에 `date -u`를 사용하므로, 이제 `date`를 명시적 사전 의존성 검사에 포함하여 복구 연습 도중 뒤늦게 실패하지 않도록 합니다.

## 검증

- `bash -n deploy/backup.sh`
- `bash -n deploy/restore.sh`
- `bash -n deploy/seed.sh`
- `git diff --check`

실제 백업 생성이나 데이터베이스 변경은 수행하지 않았습니다.

## 승격 정책

운영 승격은 Test 검증을 통과한 뒤에만 진행합니다. 이 변경은 인프라 저장소에서 별도로 진행 중인 Kubernetes-native 백업/복구 개선을 우회하지 않습니다.
