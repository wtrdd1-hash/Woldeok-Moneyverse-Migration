# Internal update v2026.09.16.144 — Admin traffic, SEO and preventive QA

- Added administrator traffic analytics with day/month/year aggregation, unique sessions, authenticated/anonymous counts, landing pages, source domains, and country summaries.
- Added Economy AI operational status to the administrator economy console.
- Found and closed a raw telemetry privilege regression: `moneyverse_app` no longer has direct table privileges on `user_activity_logs`.
- Replaced the activity-log read path with an actor-bound `admin_activity_list_logs` database function and administrator role verification.
- Refreshed canonical/search metadata and removed the member-only `/shop/catalog` route from the public sitemap.
- Expanded the machine-readable mobile API contract from 147 to 155 endpoints and advanced the contract version to `v2026.09.16.144`.
- Android QA found the Gradle wrapper executable bit missing; the app QA branch restores it and prepares v1.0.16 hardening.
- Production promotion remains blocked until full CI-equivalent, database migration, isolated Test and runtime smoke gates pass.
- Final local gate: fresh-DB migration through 202 and all 1438 database-backed backend tests passed; workspace build/lint/typecheck/tests also passed.
