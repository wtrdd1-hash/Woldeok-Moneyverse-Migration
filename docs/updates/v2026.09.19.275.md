# v2026.09.19.275 update

GitHub-facing update: adds automatic stale-build refresh and a repeatable zero-downtime host blue/green deployment path. Authentication cookies/session rows are preserved; cache refresh never deletes sessions. Test must prove the exact SHA before Production promotion.
