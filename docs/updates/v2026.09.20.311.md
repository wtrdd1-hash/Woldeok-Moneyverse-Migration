# Private chat authoritative DB core — v2026.09.20.311

## English canonical
- Priority: URGENT / P0 (`ONE_TO_ONE_PRIVATE_CHAT_SPEC` implementation order 305-02).
- Base: `main` at `9e7166a7d43ec247f52b6ecf737ed8aad83f1371`.
- Adds forward-only migration `219-private-chat-core.sql`.
- Enforces one canonical normalized participant pair, server-assigned monotonic sequence, sender/conversation-scoped idempotency with altered-body rejection, monotonic own read state, bounded text, object authorization, and private-table least privilege.
- `moneyverse_app` receives EXECUTE only on the three server contracts; it receives no direct table privileges.
- This slice intentionally does not claim block/report/realtime/notification completion; those remain gated P0 follow-ups.
- Production promotion remains blocked until real PostgreSQL, full CI/security/build and exact-SHA isolated Test gates pass.

## 한국어
- 우선순위: 긴급 / P0 (`ONE_TO_ONE_PRIVATE_CHAT_SPEC` 구현 순서 305-02).
- 기준: `main` `9e7166a7d43ec247f52b6ecf737ed8aad83f1371`.
- 순방향 전용 `219-private-chat-core.sql` 마이그레이션을 추가했다.
- 정규화된 참여자 쌍당 단일 대화, 서버 단조 증가 sequence, 발신자/대화 범위 idempotency 및 본문 변경 재시도 거부, 본인 read-state 단조 증가, 본문 길이 제한, 객체 권한검사, private table least privilege를 적용했다.
- `moneyverse_app`에는 세 서버 계약 함수 EXECUTE만 부여하고 테이블 직접 권한은 부여하지 않는다.
- block/report/realtime/notification은 아직 완료로 간주하지 않으며 후속 P0 gate로 남긴다.
- real PostgreSQL, 전체 CI/security/build, exact-SHA isolated Test가 통과하기 전 Production 승격은 차단한다.
