# 내부 업데이트 — v2026.09.15.127

## 원인
- 근무 완료는 관리자 전역 보상 cap 소진 시 DB가 `work reward quota reached`를 반환했고 Android는 HTTP 409 숫자만 노출했다.
- 카지노는 migration 195가 음수 잔액을 허용했지만 migration 171의 일 손실 1,000 / 배팅 2,000 WLD가 그대로 남아 음수 플레이가 조기에 차단됐다.

## 조치
- 서버 409 응답에 `work_reward_quota_reached`, `work_task_daily_limit_reached`, 카지노 limit/self-limit 코드를 추가한다.
- migration 198에서 플랫폼 카지노 일일 상한을 9e18/8.999e18 수준으로 되돌리고 per-play 및 사용자 self-limit은 보존한다.
- Android v1.0.14는 `/work` 대시보드 cap을 읽어 선제 차단하고 Problem Details를 한국어로 해석한다. 카지노 terms를 읽어 상태를 표시한다.
