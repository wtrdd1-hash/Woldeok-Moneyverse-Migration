# Internal update — v2026.09.19.275

Branch: `ops/blue-green-cache-refresh-v2026.09.19.275`.

Operational intent: every frontend/backend rollout uses canary-first Nginx handoff so stable units restart while traffic remains available. Frontend stale-build detection performs one cache-busted navigation while preserving cookies. Failures keep canary traffic when stable services are unhealthy.

Pre-promotion evidence: helper tests pass, frontend stale refresh 7/7, typecheck pass, real-DB authenticated session continuity pass. Exact-SHA build/Test and Production evidence are appended after runtime verification.
