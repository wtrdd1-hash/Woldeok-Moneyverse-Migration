# 내부 업데이트 — v2026.09.17.179

## 런타임 증거

- Test source/runtime: `/srv/moneyverse-data/releases/test-faa047fdb637`, 외부 `/api/version` = `faa047fdb637df74b4327ef45c9585be1c15d8c5`.
- Production frontend: `/srv/moneyverse-data/releases/prod-faa047fdb637`, 외부 `/api/version`도 동일 SHA.
- Production backend는 `/srv/moneyverse-data/releases/prod-18c7a1324013/backend` 유지. Production SHA에서 v175 SHA까지 backend/DB migration git diff는 0이다.
- Test frontend `.env.local`은 `root:root 600`에서 `root:debian 640`으로 변경했고 Test/Production Next cache 소유자는 `debian:debian`으로 교정해 런타임 EACCES를 제거했다.
- Production frontend canary는 임시 3201을 사용했고 nginx를 3001 → 3201 → 3001로 전환하면서 매 단계 설정 검사/reload를 통과했다.
- 롤백 파일: `/etc/nginx/backups/moneyverse.before-v175-prod-20260917132435`, `/etc/systemd/system/moneyverse-frontend.service.d/release.conf.before-v175-20260917132435`.

## 클러스터 장애

- GitHub 배포키 fingerprint: `SHA256:fjuCklms9aMYcahcI9yhQLsnRlOR/NXDFjmljYEPmmU`.
- `192.168.100.186`의 `root`, `nixos`, 설정된 배포 사용자와 Proxmox `192.168.100.1`의 `root`에서 직접 SSH가 모두 거부됐다.
- NixOS 소스의 `keys/admins.pub`가 placeholder만 가진 반면 비밀번호 인증은 비활성화되어 있던 것이 원인이다.
- NixOS 소스 수정은 `kuber-nixos-flakes#1`, `7a0a1438bae0b17b28b0597ea37e759ac1db6530`으로 병합했다.
- 실제 `.186` SSH는 out-of-band NixOS apply/rebuild 전까지 복구 완료로 기록하지 않는다.
- 진단용 workflow 브랜치 `fix/cluster-ssh-recovery-v2026.09.17.179`는 병합하지 않고 삭제한다.
