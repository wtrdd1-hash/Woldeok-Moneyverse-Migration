# Internal update v2026.09.17.174 — Debian automated backup system

## Scope
- Added a current Debian/systemd-native backup path instead of relying on retired Docker-host procedures.
- Added encrypted DB + production-photo backup, verification, retention, and a six-hour timer.
- Added a same-filesystem fail-closed guard and retained off-host DR as an explicit P0.

## Evidence
- DB data/photos: `/dev/sdb1`; backup target: `/dev/sda1`.
- Negative same-device test: PASS (backup blocked).
- Positive separate-device test: PASS; 9.4 MiB encrypted archive verified through SHA-256, decrypt, internal checksums, `pg_restore -l`, photo zstd integrity and format marker.

## Promotion
Install root-only key, scripts and systemd units only after branch tests/CI. Run one immediate backup, verify it, enable timer, then record timer/service evidence. Off-host immutable replication remains follow-up P0.
