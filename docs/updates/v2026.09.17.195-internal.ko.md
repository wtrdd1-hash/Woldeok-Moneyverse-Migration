# 내부 업데이트 — v2026.09.17.195

- 증거 브랜치: `docs/api-cache-production-evidence-v2026.09.17.195`
- 정확한 기준: `4ab9665bc7ae14469575b6bd9c60c2c41b16b083` (v193 병합 main).
- 수정 전 활성 Production frontend `.next/cache`, `fetch-cache`는 `root:root`였고 직전 10분 journal에 `EACCES` 16건이 있었습니다.
- 병합된 v193 helper를 `/srv/moneyverse-data/releases/prod-9e1342ca3a30-ui186`에 적용했고 cache 소유권은 `debian:debian`으로 변경됐습니다.
- frontend PID `1204581`, backend PID `400161`이 그대로 유지되어 수정 중 service 재시작이 없음을 확인했습니다.
- backend 직접 `/health`, 공개 `/health`, `/api/viewer`, `/api/wallet/summary`, `/status`, `/announcements`, `/shop` 모두 HTTP 200이었습니다.
- 수정 후 검증 구간에 새 cache `EACCES`/prerender-cache 오류와 새 backend 치명/DB 오류가 0건이었습니다.
- public NestJS `/api/v1/*` 직접 노출 금지는 의도된 구조이며 browser는 계속 Next BFF 경계를 사용합니다.
