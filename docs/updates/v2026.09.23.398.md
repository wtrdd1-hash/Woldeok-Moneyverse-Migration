# Update v2026.09.23.398 — Work reward reset planning

Planning now defines daily/weekly Work counters as server-authoritative reward windows rather than generic play caps. Reset copy must match `day_ends_at`/`week_ends_at`, finite caps require explicit protection-policy metadata, unlimited uses `null`, and web/mobile/API/settlement boundary parity is a release gate. This update changes documentation only.
