# QA Hardening — v2026.09.14.78

Date: 2026-09-14
Branch: `fix/qa-hardening-v2026.09.14.78`
Status: implementation / validation

## Scope

This update closes a restore-script dependency gap found by recurring QA. `deploy/restore.sh` generates the default restore database name with `date -u`, so `date` is now part of the explicit preflight dependency check instead of failing later during a recovery rehearsal.

## Validation

- `bash -n deploy/backup.sh`
- `bash -n deploy/restore.sh`
- `bash -n deploy/seed.sh`
- `git diff --check`

No backup was created and no database was modified.

## Promotion policy

Production promotion remains gated on Test validation. This change does not bypass the Kubernetes-native backup/recovery work tracked separately in infrastructure.
