# GitHub update — v2026.09.19.291

Member login persistence is extended to 180 days while retaining PostgreSQL as the authoritative session store. Existing active member sessions are extended during migration and refreshed browser sessions receive the longer Max-Age without replacing the login identity. Pre-login and privileged admin-session security windows are not relaxed.
