# 내부 업데이트 v2026.09.16.149

## 발견 원인

`work_complete_task_v2`와 `work_verify_and_reward`는 이미 `server_game_day_key/server_game_week_key`를 사용하지만 `work_my_dashboard`는 실제 서울 날짜와 `date_trunc('week', ...)`를 계속 사용했습니다. 이 때문에 정산 한도는 새 서버 주간으로 넘어갔어도 API/화면의 `weekly_paid`가 현실 주간 기준으로 조회되는 불일치가 있었습니다.

## 수정

신규 forward migration `200-work-dashboard-game-clock.sql`에서 live `work_my_dashboard(uuid)` 정의의 일/주 키만 서버 게임시계로 교체했습니다. 기존 069/190/193 마이그레이션은 변경하지 않았습니다.

## 회귀 방지

`server-game-clock.db.test.ts`에 현재 `server_game_day_key()` 및 `server_game_week_key()`로 reward window를 만든 뒤 `work_my_dashboard()`가 동일한 111/222 값을 반환하는 실제 PostgreSQL 테스트를 추가했습니다.

## 배포 게이트

브랜치 → CI/Build Test Candidate → isolated Test exact-SHA 및 백엔드/DB 확인 → main → 동일 SHA Production 순서를 유지합니다.
