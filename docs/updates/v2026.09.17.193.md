# v2026.09.17.193 — API-derived page cache permission hardening

- Added a guarded host-runtime helper for writable Next.js server cache preparation.
- The helper limits ownership changes to `frontend/.next/cache` and refuses paths outside the release root.
- Runtime readiness now includes an actual write probe as the frontend service user.
- The incident scope is frontend server cache freshness only; NestJS, PostgreSQL, sessions, economy data and migration 204 are unchanged.
- Test must pass backend health, BFF JSON and frontend cache checks before the same repair is applied to Production.
- Production remediation is designed to be zero-downtime and requires no backend restart.
