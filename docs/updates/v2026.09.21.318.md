# v2026.09.21.318 — Release migration sequence repair

## English canonical

- Base: latest `main` at `28d8eb48066d84e614d9da362460312d002ad207`.
- Carries the validated CI/DB/app-api release-gate repairs from the preceding B slice.
- Renumbers the not-yet-runtime-promoted private-chat migration from `219-private-chat-core.sql` to `220-private-chat-core.sql`, restoring a contiguous duplicate-free sequence without changing its SQL semantics.
- Deployment evidence was checked before the rename: the latest successful Build Production Release containing both 219 files was a non-runtime lane (`test-gate` and `build` skipped), so neither colliding 219 migration was promoted by that release path.
- Preserves immutable production migrations, least privilege, ledger integrity, and fail-closed release gates.
- Local repository tests pass, including migration parity and app-api/mobile contract checks. Real PostgreSQL, security/build, CI, exact-SHA isolated Test and Production promotion remain gated.

## 한국어

- 기준: 최신 `main` `28d8eb48066d84e614d9da362460312d002ad207`.
- 직전 개발 B의 CI/DB/app-api release gate 복구를 포함한다.
- 런타임 Production 승격 전인 private-chat migration을 `219-private-chat-core.sql`에서 `220-private-chat-core.sql`로 순번 조정해 중복 없는 연속 migration sequence를 복구했다. SQL 의미는 변경하지 않았다.
- rename 전 배포 증거를 확인했다. 두 219 파일을 포함한 최신 성공 Build Production Release는 non-runtime lane으로 `test-gate`와 `build`가 skip되어 해당 충돌 migration이 그 경로로 Production 승격되지 않았다.
- production migration 불변성, least privilege, ledger integrity, fail-closed release gate를 유지한다.
- migration parity 및 app-api/mobile contract를 포함한 로컬 repository test는 통과했다. real PostgreSQL, security/build, CI, exact-SHA isolated Test와 Production 승격은 계속 gate한다.
