# 내부 업데이트 — v2026.09.19.275

브랜치: `ops/blue-green-cache-refresh-v2026.09.19.275`.

운영 의도: 모든 frontend/backend rollout에서 canary를 먼저 기동하고 Nginx를 전환해 stable unit 재시작 중에도 트래픽을 유지합니다. 프론트 stale-build 감지는 쿠키를 유지하며 1회 cache-bust 이동합니다. stable service가 비정상이면 실패 경로에서도 canary routing을 유지합니다.

승격 전 증거: helper 테스트 통과, frontend stale refresh 7/7, typecheck 통과, 실 DB 인증 세션 연속성 통과. exact-SHA build/Test 및 Production 증거는 런타임 검증 후 추가합니다.
