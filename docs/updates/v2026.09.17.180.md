# v2026.09.17.180 — Backend session continuity

- Branch: `fix/backend-session-continuity-v2026.09.17.180`.
- Member sessions remain PostgreSQL-backed and 30-day; backend process memory is not session authority.
- Added real-DB regression coverage proving an authenticated cookie token resolves through a fresh repository/backend instance.
- Host deployment contract separates stable Production `DATABASE_URL`/secrets from immutable release identity and release working directory.
- Test runtime evidence: a prelogin cookie created before `test-main-backend.service` restart remained present in `auth_sessions` and was accepted after restart without a replacement session.
- Production rule: no session-table mutation for normal deployment/rollback; administrator console expiry remains intentionally short.

- Verification: 68/68 focused auth tests passed and backend TypeScript typecheck passed. On Test, the session token_hash row remained 1→1 across backend restart and the same cookie was accepted without issuing a replacement session.

## Final Production evidence

- PR #419 merged as `c7720282eb8bb4c672c1782861e3be251cf2bfa5`; the merged delta adds tests/docs/ops only, with no backend/frontend/package runtime source change relative to the already-Tested application runtime.
- Production backend `dist` was byte-identical to the approved runtime, so the live backend was deliberately **not restarted**. Its systemd MainPID remained unchanged during promotion.
- Stable Production DB credentials/secrets are now sourced from `/etc/moneyverse/backend-production.env`; release identity is separated into `/etc/moneyverse/backend-release.env`; the next-start code pointer is `/srv/moneyverse-data/releases/production-current/backend`.
- A browser session cookie created immediately before the Production configuration switch was accepted immediately after the switch without issuing a replacement session. The aggregate count of active authenticated sessions was unchanged before/after promotion.
- Public Production probes passed: `/health`, auth viewer, and public catalog (146 items). Backend, frontend and nginx services remained active.
- Rollback drop-in: `/etc/systemd/system/moneyverse-backend.service.d/release.conf.before-v180-20260917140053`.
