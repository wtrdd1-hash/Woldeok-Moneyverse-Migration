# 업데이트 v2026.09.25.442

- 전 사이트 QA를 route sample이 아니라 candidate의 모든 frontend page 확인으로 강화했다.
- 모든 관리자 page를 매 pass 기본 범위로 포함한다.
- 현재 source snapshot은 86개 page이며 관리자 22개, dynamic 8개다.
- candidate source에서 route inventory를 생성하고 QA ledger와 정확히 대조해야 한다.
- 모든 release candidate는 최소 5회의 전 사이트 완전 pass와 responsive, role/state, dynamic fixture, 실제 Test runtime 검증을 요구한다.
- 누락/skipped route 또는 미해결 실패가 있으면 Production을 차단한다.
- 문서 전용이며 runtime release는 수행하지 않았다.
