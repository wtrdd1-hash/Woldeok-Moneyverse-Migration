# Internal — v2026.09.17.180

Base: current `origin/main` at branch creation. Scope is backend deployment/session continuity only; no member session credential is logged or copied into Git.

Acceptance: DB-backed authenticated-session regression passes; live Test same-cookie restart check passes; Production stable env uses the existing Production DB credential; release-pointer update does not restart the live backend when the candidate backend artifact is byte-identical; public health and login/viewer paths remain healthy.

- Verification: 68/68 focused auth tests passed and backend TypeScript typecheck passed. On Test, the session token_hash row remained 1→1 across backend restart and the same cookie was accepted without issuing a replacement session.

## Final operator evidence

Production host promotion was configuration-only because backend `dist` matched the approved runtime byte-for-byte. The live backend PID was held constant; no normal member session row was revoked or rewritten. Stable Production DB credentials were copied to `/etc/moneyverse/backend-production.env` (root-only), and the reviewed systemd drop-in now targets `production-current`. A pre-change cookie remained valid after the switch and active authenticated-session aggregate count was unchanged. Public health/viewer/catalog probes passed. Exact-main verify/check for `c7720282...` passed; its immutable candidate pipeline also built the backend candidate successfully.
