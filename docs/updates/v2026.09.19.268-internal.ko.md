# 내부 승격 기록 — v2026.09.19.268

- 원본 후보: PR #541 / v2026.09.19.267.
- CI 차단 원인: migration 210이 존재하지 않는 `pg_catalog.digest(bytea, unknown)`를 호출해 real PostgreSQL 작업 테스트 5개가 실패함.
- 근본 원인: `digest`는 `pg_catalog` 함수가 아니라 이 저장소가 사용하는 pgcrypto extension 스키마의 함수임.
- 수정: `public.digest(...)`를 명시적으로 사용. economic-command 동작과 기존 정산 이력은 변경하지 않음.
- 승격 순서: 로컬 targeted test -> 전체 저장소 gate -> GitHub exact-SHA CI -> isolated Test 백엔드 health -> 운영 직접 승격.
- Git 위생: DB 런타임 데이터, dump, credential, secret은 커밋하지 않음.
