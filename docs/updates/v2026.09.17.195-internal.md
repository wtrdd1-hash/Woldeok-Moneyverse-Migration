# Internal Update — v2026.09.17.195

- Evidence branch: `docs/api-cache-production-evidence-v2026.09.17.195`
- Exact base: `4ab9665bc7ae14469575b6bd9c60c2c41b16b083` (v193 merged main).
- Before repair, the active Production frontend `.next/cache` and `fetch-cache` were `root:root`; 16 `EACCES` events were present in the preceding 10-minute journal window.
- Applied the merged v193 helper to `/srv/moneyverse-data/releases/prod-9e1342ca3a30-ui186`; cache ownership became `debian:debian`.
- Frontend PID remained `1204581`; backend PID remained `400161`, proving no service restart during the repair.
- Direct backend `/health`, public `/health`, `/api/viewer`, `/api/wallet/summary`, `/status`, `/announcements`, and `/shop` all returned HTTP 200.
- Post-repair verification showed zero new cache `EACCES`/prerender-cache errors and zero new backend fatal/database errors.
- Direct public NestJS `/api/v1/*` remains intentionally unpublished; the browser continues to use the Next BFF boundary.
