# Internal update v2026.09.16.158 — Administrator navigation inventory convergence

## Root cause

The administrator feature set was implemented, but the two navigation inventories had drifted apart. `AdminSubNav` omitted Security, Business/Season, Work/Jobs, and Discord while `ADMIN_AREAS` omitted Support and Shop. Production served the same incomplete top navigation as `main`, so this was a source/navigation defect rather than a missing backend deployment.

## Changes

- Exposed `/admin/security`, `/admin/catalog`, `/admin/work`, and `/admin/discord` in the administrator sub-navigation.
- Added `/admin/support` and `/admin/shop` to the administrator dashboard area inventory.
- Exported the tab inventory and added regression coverage requiring every top-level administrator area to be reachable from the administrator sub-navigation.
- Kept nested activity, delivery, integrity, AI-news, and scenario pages under their existing parent areas rather than duplicating every nested route in the top navigation.

## Verification

- Rechecked the latest authoritative project plan after `main` advanced to plan v2026.09.16.157; no administrator authorization, telemetry, or audit boundary conflicts were found.
- Local workspace: typecheck PASS, build PASS, lint PASS with 0 errors (11 pre-existing image optimization warnings), workspace test PASS after building the contract package first.
- Frontend: 68 files / 611 tests passed.
- Backend local non-DB run: 871 passed / 351 DB tests skipped because the local worktree did not supply the CI database environment. The GitHub candidate CI is the DB-backed authority for promotion.

## Promotion gate

Promotion remains fail-closed until the exact candidate SHA passes GitHub DB-backed CI, the isolated Test runtime serves the exact promoted SHA, the Test backend/database public-catalog probe passes, and the Test administrator HTML exposes the corrected navigation before Production promotion.
