# Internal Update — v2026.09.17.193

- Branch: `fix/frontend-api-cache-permissions-v2026.09.17.193`
- Base: `5d5826b21a9e4ae98fd0dcbe771d57fa8c674c84`
- Production backend health: PASS; no fatal backend/DB errors found in the audit window.
- One backend `StatusCollector` web timeout was observed at 18:03 KST.
- Production frontend repeatedly failed Next fetch-cache writes with `EACCES` because `.next/cache` was `root:root` while the service runs as `debian`.
- Test cache ownership is already `debian:debian` and Test has no matching error.
- Added `ops/systemd/prepare-frontend-runtime-cache.sh` plus a regression test and EN/KO operator documentation.
- Release gate: Test helper + backend health + BFF JSON + no-EACCES verification, then zero-downtime Production cache repair and smoke checks.
- Backend, database, authentication sessions and migration 204 remain outside this change.

## Test verification

- Runtime cache helper and path-boundary regression: PASS.
- Test backend health, public health/BFF, status, announcements and shop probes: HTTP 200.
- Test verification window: no new frontend cache `EACCES` and no new backend fatal/database errors.
