# Update v2026.09.25.443

- Fixed the administrator Economy Operations mobile/control-state defects reported from the live UI.
- AUTO-owned economy rates are now read-only until an explicit manual override edit is entered; min/current/max labels are unambiguous and range accessibility values derive from the same displayed state.
- AI Council cards now wrap long model/domain evidence and label model confidence explicitly, with its meaning separated from policy eligibility/evidence sufficiency.
- Fixed the administrator sub-navigation conditional-hook lint failure.
- Cleared seven pre-existing lint errors that would otherwise block the exact-SHA CI gate.
- Added exact-source full-route inventory generation; current inventory is 86 pages / 22 admin / 8 dynamic. Workflow artifact integration is separated because the current GitHub OAuth credential cannot update workflow files.
- Local validation: targeted admin regressions 12/12, frontend suite 110 files / 784 tests, lint 0 errors, typecheck passed, production build passed.
- Test/Production runtime status is recorded separately; this note does not claim promotion.
