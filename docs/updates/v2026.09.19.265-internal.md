# Internal update — v2026.09.19.265

## Runtime evidence
Test and Production both serve application source `b1b1f7aa63e630bba266c5dcbb72731faccdae8d`. Test backend/frontend are stable on 3100/3101; Production backend/frontend are stable on 3000/3001. Both databases are at migration 208.

## Incident closure
Public Test had split identity: `/api/version` and `/health` were routed to stable backend 3100 while `/` still used obsolete transient frontend 3119. This produced a current version response while BFF catalog returned HTTP 500. Nginx was corrected to stable 3101 and a repository checker now rejects transient Test frontend ports.

## Production promotion
A fresh encrypted backup and checksum were created first. Same-SHA canaries passed at 3003/3202. Nginx reload moved traffic to canaries with old stable processes still alive; stable 3000/3001 were then moved to the exact release, verified, and Nginx reloaded back. No public stop/restart window was introduced.

## Cleanup
All obsolete Test UI/canary services and temporary v263 canaries were stopped. Failed-unit count is zero. Previous release directories/config backups remain available for rollback.
