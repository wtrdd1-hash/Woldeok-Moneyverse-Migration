# 내부 업데이트 — v2026.09.19.291

## 구현
- 쿠키 SESSION_MAX_AGE: 15,552,000초(180일).
- SessionRepository.rotateCsrf: 일반 회원 DB 만료를 180일 sliding으로 연장하고 anonymous pre-login은 30일 유지.
- migration 215-member-session-180-day-persistence.sql: OAuth/로컬 이메일/mobile handoff 등 모든 회원 세션 INSERT 경로에서 일반 회원 만료를 최소 180일로 정규화하고 현재 활성 비관리자 회원 세션도 연장.
- 관리자 세션은 admin_opened_at IS NULL 조건으로 명시적으로 제외.

## 필수 증거
1. 집중 unit test와 backend typecheck.
2. migration/DB test.
3. exact-SHA Test 배포.
4. 백엔드 재시작 전에 인증된 회원 cookie/session을 확보하고 재시작 뒤 동일 세션이 그대로 승인되는지 확인.
5. Test API/health/smoke 통과.
6. 이후에만 무중단 Production 승격하고 exact SHA와 세션 연속성을 다시 확인.
