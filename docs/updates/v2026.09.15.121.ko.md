# v2026.09.15.121 — WLD 대용량 정밀 저장

## 적용 범위
- 지갑 송금, 은행 이동, 대출 상환, 공통 금액 계약에서 구현상 금액 크기 상한을 제거합니다.
- 핵심 원장 잔액/전표와 은행 금액 컬럼을 `bigint` / `numeric(38,0)`에서 PostgreSQL 임의 정밀도 정수 `numeric`으로 전환합니다.
- API의 WLD는 10진 정수 문자열로 유지해 JavaScript `Number.MAX_SAFE_INTEGER`를 넘어도 반올림되지 않게 합니다.
- 하드코딩된 대출 요청 500,000 WLD, 은행 이동/상환 1,000,000,000 WLD 기술 상한은 제거합니다. 신용등급·카지노·주식 위험관리 등 게임/안전 정책 한도는 유지합니다.

## 호환성·안전
- 기존 내부 safe integer 호출은 호환되지만 공개 DTO는 10진 문자열을 요구합니다.
- 원장 primitive에 대한 런타임 DB 역할 접근은 계속 차단합니다.
- 멱등성, 음수잔액 방지, 신용등급 대출정책, 계정 잠금 규칙은 유지합니다.
- 원장 금액 타입에 의존하던 뷰는 migration 안에서 동일 정의로 재생성합니다.

## 검증
- migration 193까지 fresh PostgreSQL 전체 migration 성공.
- backend DB 포함 1,429 / 1,429 테스트 통과.
- 승격 전 workspace typecheck 통과.
