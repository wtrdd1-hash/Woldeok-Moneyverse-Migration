# Internal Update — v2026.09.17.179

## Runtime evidence

- Test source/runtime: `/srv/moneyverse-data/releases/test-faa047fdb637`; external `/api/version` = `faa047fdb637df74b4327ef45c9585be1c15d8c5`.
- Production frontend: `/srv/moneyverse-data/releases/prod-faa047fdb637`; external `/api/version` = the same SHA.
- Production backend remains `/srv/moneyverse-data/releases/prod-18c7a1324013/backend`; git diff confirms zero backend/database migration changes from Production SHA to v175 SHA.
- Test frontend `.env.local` was changed from `root:root 600` to `root:debian 640`; Next cache ownership for Test and Production was changed to `debian:debian` to remove runtime EACCES.
- Production frontend canary: temporary port 3201; nginx switched 3001 → 3201 → 3001 with successful config tests and reloads.
- Rollback files: `/etc/nginx/backups/moneyverse.before-v175-prod-20260917132435` and `/etc/systemd/system/moneyverse-frontend.service.d/release.conf.before-v175-20260917132435`.

## Cluster incident

- GitHub deploy key fingerprint: `SHA256:fjuCklms9aMYcahcI9yhQLsnRlOR/NXDFjmljYEPmmU`.
- Direct SSH failed for `root`, `nixos`, configured deploy user on `192.168.100.186`, and for `root` on Proxmox `192.168.100.1`.
- Root cause in NixOS source: `keys/admins.pub` contained only the repository placeholder while password authentication is disabled.
- NixOS source fix merged via `kuber-nixos-flakes#1` as `7a0a1438bae0b17b28b0597ea37e759ac1db6530`.
- Live `.186` SSH is not claimed recovered until an out-of-band NixOS apply/rebuild occurs.
- Diagnostic workflow branch `fix/cluster-ssh-recovery-v2026.09.17.179` must be deleted, not merged.
