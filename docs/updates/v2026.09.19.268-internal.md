# Internal release record — v2026.09.19.268

- Source candidate: PR #541 / v2026.09.19.267.
- CI blocker: five real-PostgreSQL work tests failed because migration 210 called nonexistent `pg_catalog.digest(bytea, unknown)`.
- Root cause: `digest` is provided by pgcrypto in the extension schema used by this repository, not `pg_catalog`.
- Fix: use explicit `public.digest(...)`; no economic-command behavior or settled-history rewrite.
- Promotion sequence: local targeted test -> full repository gates -> GitHub exact-SHA CI -> isolated Test backend health -> direct Production promotion.
- Git hygiene: database runtime data, dumps, credentials, and secrets are not committed.
