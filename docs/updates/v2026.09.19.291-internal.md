# Internal update — v2026.09.19.291

## Implementation
- Cookie SESSION_MAX_AGE: 15,552,000 seconds (180 days).
- SessionRepository.rotateCsrf: ordinary members slide DB expiry by 180 days; anonymous pre-login remains 30 days.
- Migration 215-member-session-180-day-persistence.sql: BEFORE INSERT trigger normalizes ordinary member session expiry to at least 180 days across OAuth, local-email and mobile handoff issuance paths; currently active non-admin member rows are extended.
- Administrator sessions are explicitly excluded by admin_opened_at IS NULL.

## Required evidence
1. Focused unit tests and backend typecheck.
2. Migration/DB tests.
3. Exact-SHA Test deployment.
4. Capture one authenticated member cookie/session before backend restart; prove the identical session remains accepted after restart.
5. Test API/health/smoke pass.
6. Only then zero-downtime Production promotion; verify exact SHA and session continuity again.
