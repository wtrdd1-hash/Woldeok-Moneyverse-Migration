# 내부 업데이트 — v2026.09.17.193

- 브랜치: `fix/frontend-api-cache-permissions-v2026.09.17.193`
- 기준: `5d5826b21a9e4ae98fd0dcbe771d57fa8c674c84`
- Production backend health는 정상이며 점검 구간에 치명적인 backend 또는 DB 오류는 확인되지 않았습니다.
- backend `StatusCollector`의 web timeout 1건이 18:03 KST에 확인됐습니다.
- Production frontend는 service가 `debian`으로 실행되는데 `.next/cache`가 `root:root`라 Next fetch-cache 쓰기가 `EACCES`로 반복 실패했습니다.
- Test cache는 이미 `debian:debian`이며 같은 오류가 없습니다.
- `ops/systemd/prepare-frontend-runtime-cache.sh`, 회귀 테스트, EN/KO 운영 문서를 추가했습니다.
- 릴리스 gate는 Test helper, backend health, BFF JSON, no-EACCES 확인 후 Production 캐시 무중단 수정과 smoke 검사입니다.
- backend, database, 사용자 데이터, migration 204는 이번 변경 범위 밖이며 그대로 유지합니다.

## Test 검증

- runtime cache helper와 경로 경계 회귀 검사: PASS.
- Test backend health, public health/BFF, status, announcements, shop probe: HTTP 200.
- Test 검증 구간: 새 frontend cache `EACCES` 및 새 backend 치명/DB 오류 없음.
