# v2026.09.17.180 — 백엔드 로그인 세션 연속성

- 브랜치: `fix/backend-session-continuity-v2026.09.17.180`.
- 일반 회원 로그인은 PostgreSQL 세션과 30일 수명을 유지하며 백엔드 프로세스 메모리에 의존하지 않습니다.
- 로그인된 세션 토큰이 새 repository/백엔드 인스턴스에서도 그대로 조회되는 실 DB 회귀 테스트를 추가했습니다.
- 호스트 배포에서는 Production `DATABASE_URL`/비밀값을 immutable release 식별자 및 작업 디렉터리와 분리합니다.
- Test 실증: `test-main-backend.service` 재시작 전 생성한 쿠키의 `auth_sessions` 행이 재시작 후에도 유지됐고 새 세션 발급 없이 그대로 승인됐습니다.
- 일반 배포/롤백은 세션 테이블을 변경하지 않으며 관리자 콘솔의 짧은 만료시간은 그대로 유지합니다.

- 검증: 인증 집중 테스트 68/68 통과, backend TypeScript typecheck 통과. Test에서 세션 token_hash 행은 백엔드 재시작 전후 1→1로 유지됐고 동일 쿠키가 새 세션 발급 없이 승인됐습니다.

## 최종 Production 증거

- PR #419는 `c7720282eb8bb4c672c1782861e3be251cf2bfa5`로 병합됐으며 병합 변경은 테스트/문서/운영 설정뿐이고 이미 Test된 애플리케이션 런타임 대비 backend/frontend/packages 실행 소스 변경은 없습니다.
- Production backend `dist`가 승인 런타임과 byte-identical이어서 live backend를 의도적으로 **재시작하지 않았습니다**. 승격 중 systemd MainPID도 그대로 유지됐습니다.
- Production DB 자격증명/비밀은 `/etc/moneyverse/backend-production.env`, 릴리스 식별자는 `/etc/moneyverse/backend-release.env`, 다음 시작 코드 포인터는 `/srv/moneyverse-data/releases/production-current/backend`로 분리했습니다.
- Production 설정 전환 직전에 만든 브라우저 세션 쿠키가 전환 직후 새 세션 발급 없이 그대로 승인됐습니다. 활성 로그인 회원 세션의 집계 수 또한 승격 전후 동일했습니다.
- 공개 Production `/health`, 인증 viewer, 공개 카탈로그(146개) 검증을 통과했고 backend/frontend/nginx 서비스가 모두 active 상태를 유지했습니다.
- 롤백 drop-in: `/etc/systemd/system/moneyverse-backend.service.d/release.conf.before-v180-20260917140053`.
