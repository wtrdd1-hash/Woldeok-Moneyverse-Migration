# 내부용 — v2026.09.17.180

기준: 브랜치 생성 시점 최신 `origin/main`. 범위는 백엔드 배포/로그인 세션 연속성이며 회원 세션 자격증명은 로그나 Git에 저장하지 않습니다.

승인 조건: DB 기반 로그인 세션 회귀 테스트 통과, Test 동일 쿠키 재시작 검증 통과, Production 고정 env가 기존 Production DB 자격증명을 그대로 사용, 백엔드 artifact가 byte-identical이면 live backend 재시작 없이 release pointer만 갱신, 공개 health/login/viewer 경로 정상 유지.

- 검증: 인증 집중 테스트 68/68 통과, backend TypeScript typecheck 통과. Test에서 세션 token_hash 행은 백엔드 재시작 전후 1→1로 유지됐고 동일 쿠키가 새 세션 발급 없이 승인됐습니다.

## 최종 운영 증거

Production host 승격은 backend `dist`가 승인 런타임과 byte 단위로 동일했기 때문에 설정 전환만 수행했습니다. live backend PID를 유지했고 일반 회원 세션 행을 revoke/재작성하지 않았습니다. Production DB 자격증명은 root 전용 `/etc/moneyverse/backend-production.env`로 고정했고 검토된 systemd drop-in은 `production-current`를 사용합니다. 전환 전 쿠키가 전환 후에도 유효했고 활성 로그인 세션 집계 수가 동일했습니다. 공개 health/viewer/catalog 검증도 통과했습니다. `c7720282...` exact-main verify/check는 통과했고 immutable candidate 파이프라인의 backend candidate build도 성공했습니다.
