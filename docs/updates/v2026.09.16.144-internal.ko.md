# 내부 업데이트 v2026.09.16.144 — 관리자 접속 분석, SEO, 사전 차단 QA

- 관리자에서 일/월/년 접속량, 고유 세션, 로그인/비로그인 세션, 최초 진입 페이지, 유입 도메인, 국가 집계를 확인할 수 있게 했습니다.
- 관리자 경제 화면에 Economy AI 전체 운영 상태를 추가했습니다.
- 원시 텔레메트리 권한 회귀를 발견해 차단했습니다. `moneyverse_app`은 더 이상 `user_activity_logs` 테이블을 직접 읽거나 수정할 수 없습니다.
- 활동 로그 조회를 actor가 포함된 `admin_activity_list_logs` DB 함수로 교체하고 DB에서도 관리자 역할을 다시 검증합니다.
- canonical/검색 메타데이터를 최신화하고 회원 전용 `/shop/catalog`을 공개 sitemap에서 제거했습니다.
- 기계 판독 모바일 API 계약을 147개에서 155개 endpoint로 확장하고 계약 버전을 `v2026.09.16.144`로 올렸습니다.
- Android QA에서 Gradle wrapper 실행 권한 누락을 발견했고 앱 QA 브랜치에서 복구 및 v1.0.16 하드닝을 준비했습니다.
- 전체 CI, DB migration, 격리 Test, 런타임 smoke가 모두 통과하기 전에는 Production 승격하지 않습니다.
- 최종 로컬 게이트에서 빈 DB 기준 migration 202까지 적용과 DB 포함 backend 1438개 전체 테스트가 통과했고, workspace build/lint/typecheck/test도 통과했습니다.
