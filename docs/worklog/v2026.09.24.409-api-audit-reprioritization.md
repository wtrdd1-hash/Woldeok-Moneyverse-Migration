# API Audit and Implementation Reprioritization Worklog — v2026.09.24.409

**Status:** PLANNING
**Start baseline:** `origin/main=b8bfc22ddff09f5ccd7632024deac447ff159ff6`
**Branch:** `planning/api-audit-reprioritization-v2026.09.24.409`
**Scope:** planning/documentation only; no runtime, Test, or Production implementation is claimed.

## Start record

The API audit was opened because current documentation contains multiple incompatible inventory statements: the machine-readable mobile contract contains 179 endpoints; the complete mobile API spec states 57 backend controllers / 335 backend endpoints / 179 mobile endpoints; the older endpoint catalog still states 139 mobile endpoints; the v402 source scan separately recorded 58 controller files / 361 HTTP decorators. These values are not treated as equivalent counting methods.

Latest-main CI evidence was reviewed. The immediately preceding runtime-check failed before API contract verification due to ESLint errors, so there is no accepted exact-SHA proof that the current main API contract check passes end-to-end.

## Planning decision

Implementation priority is reset around generated-contract authority and risk:
1. restore required CI gates;
2. generate and validate the API contract on the exact candidate SHA;
3. derive authoritative method+path inventory and reconcile all API documentation;
4. audit authn/authz, CSRF, recent-reauth, idempotency, concurrency and error contracts;
5. verify high-risk money/state mutations first;
6. verify realtime/chat and mobile/web BFF parity;
7. run exact-SHA Test E2E;
8. allow Production promotion only after all blockers close.

## Evidence boundary

This cycle records planning changes only. It does not claim that 335, 361, or any other backend endpoint count is the new authority until generated semantic inventory is produced and verified.

## Completion record

Updated PROJECT_PLAN and INTEGRATED_PLANNING_MASTER to v2026.09.24.409, added the EN/KO planning delta, synchronized the documentation index authority marker, and added GitHub-facing update notes. The implementation order is now explicitly gated from CI recovery through generated contract authority and exact-SHA Test E2E before Production eligibility.

No application code, Test runtime, database, or Production environment was changed in this documentation cycle.
