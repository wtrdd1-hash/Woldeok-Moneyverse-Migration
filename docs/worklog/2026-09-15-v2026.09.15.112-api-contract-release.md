# v2026.09.15.112 — Mobile API contract release repair

## Internal / GitHub update

- Release validation on `debian13` found generated mobile API contract artifacts drifting from current controller symbols on application `main`.
- Regenerated `docs/mobile-api-contract.json` and the English/Korean schema references from the current source.
- The regenerated contract contains 139 enriched endpoints and is now stable under the repository contract drift check.
- Validation on the miniPC release worktree passed backend and frontend tests, workspace typecheck, and production build.
- This release does not bypass the isolated-test gate. The exact merged SHA must pass the test runtime before production promotion.

## Validation

- Backend: 856 executed tests passed; DB-dependent tests requiring the dedicated runtime remain separately gated.
- Frontend: 593 tests passed.
- Workspace typecheck: passed.
- Production build: passed.
