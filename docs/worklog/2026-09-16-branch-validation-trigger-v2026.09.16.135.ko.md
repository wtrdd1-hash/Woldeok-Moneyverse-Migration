# 브랜치 검증 트리거 범위 보강 — v2026.09.16.135

## 범위

v2026.09.16.134 후속 수정입니다. 대기열 드레인 자체는 고쳤지만, `Build Test Candidate` push 필터에 포함되지 않은 개발 브랜치 접두사와 정확한 현재 HEAD에 성공 후보 실행이 없는 브랜치는 계속 남을 수 있었습니다.

## 변경 내용

1. `Build Test Candidate` push 대상에 `feature/**`, `bugfix/**`, `security/**`를 추가했습니다.
2. 자동 통합 스캔이 대상 브랜치의 정확한 HEAD SHA에 성공 후보 실행이 없는 것을 발견하면 단순 건너뛰지 않고 해당 브랜치로 `test-candidate.yml`을 실행합니다.
3. 운영 가시성을 위해 워크플로 요약에 검증 실행 요청 건수를 기록합니다.
4. 정확한 SHA 병합 게이트, 충돌 브랜치 건너뛰기, 최종 main 후보 실행, 격리 Test 검증, 운영 승격 게이트는 그대로 유지합니다.

## 기대 결과

관리 대상 모든 브랜치 접두사는 새 push 시 자동 검증됩니다. 과거 브랜치나 아직 검증되지 않은 대상 브랜치는 시간당 통합 스캔에서 후보 테스트가 자동 요청되고, 성공 완료 시 이벤트 기반 통합 워크플로가 즉시 다시 실행됩니다.
