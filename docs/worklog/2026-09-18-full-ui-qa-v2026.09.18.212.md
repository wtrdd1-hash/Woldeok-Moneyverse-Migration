# Full UI QA worklog — v2026.09.18.212

## Audit scope

The v211 authoritative plan and exact main were checked before work and rechecked mid-work. UI review covered responsive width constraints, tables/dialogs, touch targets, mobile input font sizing, browser errors, CSP, and metadata title composition.

## Findings and actions

- Preserved intentional table/policy/casino internal horizontal scrollers.
- Reproduced and fixed 280 px deletion/legal/shop page overflow from grid min-content behavior.
- Reproduced and fixed Gallery AdSense `sodar2.js` CSP refusal with a narrow auxiliary-script allowlist.
- Reproduced and fixed duplicated browser titles caused by branded child metadata plus the root template.
- Reproduced 14 px shop search input on phones and raised its mobile size to 16 px.
- Local-canary activity telemetry 503 was traced to the non-public `127.0.0.1:3309` origin; direct Test backend activity ingestion returned HTTP 200 / recorded=1, so no telemetry code was changed for a local-host artifact.

## Evidence

Targeted tests 15/15, frontend 71/623, typecheck/build pass, lint 0 errors. Full responsive automation exercised 300 route/width combinations; three login redirect races were individually rerun and passed. Final runtime acceptance remains Test-first after PR merge.
