# 2026-09-14 — 전체 브랜치 통합 v2026.09.14.83

## 범위

갱신된 `main` 기준으로 활성 개발 브랜치를 통합했으며 `main` 강제 푸시는 사용하지 않았다.

## 통합 판단

- `integrate/stock-comparison-next-v2026.09.14.82`를 주식 기능 누적 구현 기준으로 사용했다.
- 구형 인증 단순화 브랜치와 충돌한 부분은 더 최신인 `main` 인증 코드/문서를 유지했다. 해당 비밀번호 정책 동작은 이미 `main`에 포함되어 있었다.
- 브라우저 간 이메일 인증용 DB 마이그레이션과 프론트/백엔드 흐름을 병합했다.
- 모바일 사업 카탈로그 호환성 수정 커밋은 갱신된 `main`에 이미 포함된 것을 확인했다.
- 검증 기반 자동 통합/승격 워크플로를 추가했다.
- 이전 브랜치에만 남아 있던 종목 상세 v2026.09.14.75 문서 재동기화 커밋을 재적용했다.

## 검증 및 배포

이 커밋 이후 로컬 CI 동등 검증, 브랜치 Test Candidate 빌드, 격리 테스트 서버 동일 SHA/백엔드 스모크, Production 승격 및 배포 후 스모크를 순서대로 수행한다.
