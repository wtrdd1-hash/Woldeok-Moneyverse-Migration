# Development B worklog — v2026.09.23.402

- Base: `main` at `7b705e1d37e97ccd05ba12042c3fd8d582e396d0`.
- Scope: P0 required-CI runtime-check unblock, avoiding open Board/Business/App work.
- Root cause: `bot/scripts/qa_ads_sponsorblock_verification.mjs` contained literal newlines inside single-quoted strings and a nested single quote in the ffmpeg `aselect` command, so repository ESLint stopped at a parser error before later gates.
- Change: repair newline literals, use template literals for shell commands that contain single quotes, remove an unused voice import, and remove an unused catch binding. QA behavior and SponsorBlock categories are unchanged.
- Focused validation: `node --check`, ESLint on the repaired script, and `git diff --check` pass.
- Repository-wide lint remains red from unrelated pre-existing debt, so typecheck/build/database/test/audit remain blocked in required CI. No merge or Production promotion is allowed from this worklog alone.
