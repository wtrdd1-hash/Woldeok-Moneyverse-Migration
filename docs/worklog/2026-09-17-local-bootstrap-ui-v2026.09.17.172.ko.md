# 내부 작업 기록 — v2026.09.17.172

범위: Bootstrap 로컬 적용 및 공통 UI 개선.

- `origin/main`의 `d398f6e`에서 분리된 작업트리로 시작했습니다.
- 구현 전과 작업 중간에 최신 초안 기획 브랜치 `docs/plan-v171`을 다시 확인했습니다. 런타임 변경은 브랜치 → CI → exact-SHA 테스트 → main → 운영 순서를 유지합니다.
- Bootstrap 5.3.8 컴파일 배포본을 보조 드라이브에 다운로드하고 SHA-256을 검증했습니다.
- `bootstrap@5.3.8`을 프론트엔드 워크스페이스에 추가해 Next 빌드 안에 로컬 번들되도록 했으며 jsDelivr/CDN URL은 추가하지 않았습니다.
- 비즈니스 로직이나 백엔드 API는 건드리지 않고 셸·카드·버튼 공통 표현만 개선했습니다.
- 검증: diff 검사 통과, 타입 검사 통과, 프론트엔드 614/614 테스트 통과, 운영 빌드 통과.
- 깨끗한 작업트리에서 최초 프론트 타입 검사는 `@moneyverse/contract` 미빌드 때문에 실패했으며 contract 빌드 후 정상 통과했습니다.
- DB 마이그레이션 없음. 백엔드 동작 변경 없음.
- 롤백: 이번 버전 커밋을 되돌리면 됩니다. 보조 드라이브의 원본 ZIP은 비활성 소스 자료로 남겨도 됩니다.
