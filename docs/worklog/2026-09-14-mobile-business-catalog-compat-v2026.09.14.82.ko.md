# 내부 작업 기록 — v2026.09.14.82

## 범위
모바일 앱 사업 카드에서 구매가격과 예상수익이 `null`로 표시되는 문제를 기존 웹/백엔드 계약을 깨지 않고 수정합니다.

## 원인
백엔드 사업 카탈로그는 이미 정식 camelCase 필드(`purchaseCost`, `dailyRevenue`, `dailyOperatingCost`)를 정상 반환합니다. 배포된 모바일 클라이언트가 가격/수익의 구형 의미 기반 호환 필드를 읽어 이름은 표시되지만 숫자 값이 null로 처리됩니다.

## 변경
공개 App API 게이트웨이 응답 변환에서만 호환 별칭을 추가합니다. 백엔드 정식 DTO와 데이터베이스 값은 변경하지 않습니다.

## 승격 순서
1. 브랜치 CI / lint / typecheck / test / build.
2. 테스트 후보 이미지를 배포합니다.
3. 격리 테스트 서버에서 동일 SHA와 백엔드/API 응답을 확인합니다.
4. 검증 후에만 `main`에 병합합니다.
5. 기존 exact-SHA 운영 게이트와 GitOps 승격 체인으로 운영에 반영합니다.
