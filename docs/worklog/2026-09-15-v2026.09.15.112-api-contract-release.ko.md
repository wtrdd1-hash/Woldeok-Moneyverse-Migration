# v2026.09.15.112 — 모바일 API 계약 릴리스 복구

## 내부/GitHub 업데이트 내역

- `debian13`에서 릴리스 검증 중 애플리케이션 `main`의 현재 컨트롤러 심볼과 생성된 모바일 API 계약 문서가 서로 다른 것을 확인했다.
- 현재 소스를 기준으로 `docs/mobile-api-contract.json`과 영어/한국어 스키마 참조 문서를 다시 생성했다.
- 재생성된 계약은 139개 엔드포인트를 포함하며 저장소의 API 계약 drift 검사와 일치한다.
- 미니PC 릴리스 worktree에서 백엔드/프론트 테스트, 전체 typecheck, production build를 통과했다.
- 격리 테스트 게이트는 우회하지 않는다. 최종 병합 SHA가 테스트 런타임을 통과한 뒤에만 운영으로 승격한다.

## 검증 결과

- 백엔드: 실제 실행 테스트 856개 통과. 전용 DB가 필요한 테스트는 별도 런타임 게이트에서 검증한다.
- 프론트엔드: 593개 테스트 통과.
- 전체 typecheck: 통과.
- production build: 통과.
