# Backend lint cleanup v2026.09.22.351

Latest main still had six backend ESLint errors. Two are already owned by open Development B PRs (#660 admin pool typing, #662 chat controller), so this branch fixes only the four non-overlapping chat/club stale imports.

Validation: targeted ESLint passed; backend TypeScript typecheck passed; backend Vitest passed 84 files / 931 tests with DB-dependent suites skipped by the local environment; git diff --check passed. No schema or economic state changes.
