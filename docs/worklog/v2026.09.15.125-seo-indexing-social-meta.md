# SEO indexing and social metadata hardening — v2026.09.15.125

## Status
In progress.

## Scope
- Reconcile the reported Production SEO findings with current main.
- Preserve environment-controlled indexing: Production indexable, isolated Test noindex.
- Improve public metadata description and social preview metadata.
- Add a 1200x630 social preview image.
- Verify robots.txt, sitemap.xml, locale metadata, home H1, and crawlable public copy.
- Validate locally, then use the repository Test → Production promotion gates.

## Checklist
- [x] Re-read `AGENTS.md`, current SEO planning spec, `PROJECT_PLAN.md`, and release guide before implementation.
- [x] Confirm current main already implements dynamic robots/sitemap and locale-aware `<html lang>`.
- [ ] Implement metadata/social preview changes.
- [ ] Re-check planning documents midway before validation.
- [ ] Run lint, typecheck, tests, and build.
- [ ] Push branch and obtain exact-SHA Test Candidate validation.
- [ ] Verify isolated Test runtime, backend/database smoke path, and noindex boundary.
- [ ] Integrate to main and verify Production promotion/runtime SEO endpoints.
- [ ] Add final English/Korean update notes and deployment evidence.

## Initial findings
The external report is partly stale relative to current `main`: `robots.ts` and `sitemap.ts` exist, and `<html lang>` is locale-aware. The remaining confirmed code gap is missing Open Graph/Twitter images and a small Twitter card. Production indexing still depends on `SEO_INDEXING_ENABLED=true`, so runtime verification is mandatory rather than hardcoding indexing on all environments.
