# Audit privileged-operation step-up — v2026.09.22.365

## English canonical

### Scope
Development B / backend security. Current main was inspected together with open hourly branches. Existing stock, shop, work-policy, role, safety, AI-news, and lint PR scopes were excluded.

### Change
- Added `ReauthGuard` to audit-event reveal because it unmasks protected audit data.
- Added `ReauthGuard` to retention-policy mutation and disposition recording because they govern retention/destruction decisions.
- Kept hash-chain verification at the existing admin-session + CSRF boundary because it verifies integrity without revealing masked fields or changing retention policy.
- No API payload, migration, database privilege, audit-chain, or ledger mutation was changed.

### Validation
Focused guard test, backend typecheck, changed-file lint, and diff check are required before PR publication. Repository required CI, real PostgreSQL gate, and exact-SHA isolated Test remain release blockers.

## 한국어
감사 이벤트 공개와 보존 정책/처분 변경에 최근 재인증을 추가했습니다. 기존 감사 체인 검증과 API/DB/ledger 계약은 변경하지 않았습니다. required CI, real PostgreSQL, exact-SHA isolated Test가 모두 성공하기 전에는 병합/Production 승격하지 않습니다.
