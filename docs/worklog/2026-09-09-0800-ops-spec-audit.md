# Hourly operations/specification audit — 2026-09-09 08:00 KST

## Scope

Review the current `main`, open pull requests, CI state, deployment workflow, Living Spec, public production surface, release documentation, and operational controls for drift since the previous audit.

## References checked

- GitHub protected-branch documentation: required status checks prevent merges until selected checks succeed.
- Kubernetes deployment documentation: rollout status/history and explicit rollback are the supported primitives for controlled Deployment updates.
- Docker Build documentation: provenance and SBOM attestations are available for `docker/build-push-action` and should be considered for image supply-chain evidence.
- Current repository `README/README.ko.md`, `AGENTS.md`, Living Project Plan, CI/deploy workflows, PRs and issues.
- Public `https://easy-scraping.com/` production page.

## Findings

### P1 — repository deployment workflow is not the production runtime contract

Tracked by issue #126. The repository still ships an SSH + Docker Compose rollout even though the recorded production completion path uses Kubernetes/containerd and Flux. PR #125 adds backup/data-audit gates to the Docker Compose path, so despite green CI it must not be merged as the production safety solution until #126 is resolved or the PR is retargeted to the Kubernetes/GitOps contract.

Action this run: added an explicit blocking audit comment to PR #125.

### P1 — `main` protection does not require CI

The current branch response reports `protected=true` but required status-check enforcement is `off` and the required check lists are empty. This conflicts with the project's mandatory CI-before-merge rule. The CI workflow already has one comprehensive `check` job covering secrets, lint, typecheck, build, PostgreSQL migrations, database-backed tests, Prisma migration guards, and production dependency audit.

Action this run: opened issue #127 to require the CI `check` context (preferably strict/up-to-date) and preserve no-force-push/no-delete rules.

### P2 — stale deployment history in SEO/ads worklog

The merged SEO/ads worklog still stated in present tense that `RELEASING.md` and `deploy.yml` attempted to deploy the retired `wdmv` test stack. Current `deploy.yml` is production-only (`wdmvp`); the real remaining drift is the Docker Compose transport versus Kubernetes/Flux runtime.

Action this run: corrected the historical wording and linked the active discrepancy to issue #126.

### P2 — image attestations are explicitly disabled

The current build workflow sets both `provenance: false` and `sbom: false` for production images. Docker's current guidance supports provenance and SBOM attestations with `docker/build-push-action`. This is a supply-chain evidence gap, but it was not changed in this run because the authoritative production deployment path is already under redesign in #126; the attestation change should land with or immediately after that workflow reconciliation so the resulting Kubernetes/GitOps consumer can verify the exact image evidence.

## Validation

- PR #125 head `579b2874...`: GitHub Actions CI completed successfully.
- Current `main` observed at the start of this run: `8041b6a7063730e9e8d71925a8e684afe62039c0`.
- Public production home was reachable and rendered the Woldeok Moneyverse page during this audit.
- No dedicated isolated test server was proven usable in this run; no test-server pass is claimed.

## Changes

- Corrected `docs/worklog/2026-09-08-seo-ads-legal-audit.md` so historical retired-test-stack wording no longer describes the current workflow incorrectly.
- Added this audit log.
- Added audit comment to PR #125.
- Opened issue #127 for CI-enforced branch protection.

## Merge/deployment status

This documentation correction remains on a non-main audit branch. No production deployment is required for the documentation-only correction. PR #125 remains open and should not merge until its deployment integration matches #126.

## Backup/rollback

No database, runtime, or production data change was made. Rollback for this run's repository change is a normal documentation commit revert.

## Remaining priority

1. Resolve #126 by making Kubernetes/Flux the single authoritative deployment contract and preserve exact-image rollback/readiness checks.
2. Enforce required CI status checks on `main` (#127).
3. Retarget the useful read-only data-integrity audit from PR #125 to the supported production runtime.
4. Add image provenance/SBOM evidence and verification to the authoritative image build/deploy path.
