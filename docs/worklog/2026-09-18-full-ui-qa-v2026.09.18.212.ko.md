# 전체 UI QA 작업일지 — v2026.09.18.212

## 감사 범위

작업 전과 중간에 v211 권위 기획서와 exact main을 재확인했습니다. 반응형 폭, table/dialog, 터치 target, 모바일 입력 글꼴, browser 오류, CSP, metadata title 조합을 점검했습니다.

## 발견 및 조치

- 공통 Table, policy table, casino tab 같은 의도된 내부 가로 스크롤은 유지했습니다.
- 삭제 안내/법적 문서/상점의 280px page overflow를 재현하고 grid min-content 동작을 수정했습니다.
- Gallery AdSense `sodar2.js` CSP 거부를 재현하고 필요한 Google 보조 script origin만 추가했습니다.
- child metadata의 brand와 root template 결합으로 browser title이 중복되는 문제를 재현하고 수정했습니다.
- 휴대폰 상점 검색 입력 14px을 재현하고 16px로 수정했습니다.
- local canary activity telemetry 503은 비공개 `127.0.0.1:3309` origin 차이로 추적했고 Test backend 직접 activity ingest는 HTTP 200 / recorded=1이므로 telemetry 코드는 변경하지 않았습니다.

## 증거

타깃 테스트 15/15, frontend 71/623, typecheck/build 통과, lint 오류 0건. 전체 반응형 자동화 300 route/width 조합을 수행했고 login redirect race 3건은 개별 재실행 통과했습니다. 최종 runtime 수용은 PR 병합 뒤 Test 우선으로 진행합니다.
