# v2026.09.16.149 — Work dashboard reset convergence

## Summary

- Fixed the Work dashboard read model using the real Asia/Seoul calendar while authoritative Work settlement used the accelerated Moneyverse clock.
- Daily Work usage now reads the same `server_game_day_key()` used by settlement.
- Weekly Work usage now reads the same `server_game_week_key()` used by settlement.
- Added a real-database regression test proving the dashboard resolves current server-day and server-week reward windows.

## User impact

The Work dashboard no longer keeps stale weekly usage until the real-world Monday boundary. With the current clock policy, one server day is 10 real minutes and one server week is 70 real minutes.

## Safety

This is a forward-only read-model correction. Existing reward receipts, profession mastery, idempotency, administrator quotas, and settlement ledgers are not rewritten.
