# v2026.09.19.275 — Blue/green continuity and automatic latest-build refresh

## Summary
Runtime deployments now have a repeatable host blue/green handoff and visible stale tabs automatically move to the new frontend build without clearing login cookies.

## Changes
- Automatic one-shot cache-busted refresh when frontend build identity changes.
- Reload-loop guard with manual fallback only when an intermediary cache still serves the old shell.
- Production release environment generator with stable secret/release separation.
- Nginx server-block port switcher with fail-closed parsing.
- Host blue/green promotion helper: canary health/version -> Nginx handoff -> stable systemd restart -> stable health/version -> Nginx return.
- Failure path preserves canary routing when primary recovery fails, avoiding intentional downtime.

## Validation before runtime promotion
- stale-tab frontend regression: 7/7 passed.
- frontend typecheck passed.
- Nginx switch helper regression passed.
- Test/Production release-env helper regressions passed.
- Real PostgreSQL authenticated-session continuity test passed.

## Deployment status
- Isolated Test: pending exact-SHA build/promotion.
- Production: pending merged exact-SHA Test evidence.
