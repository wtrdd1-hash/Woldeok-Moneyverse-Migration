# v2026.09.16.155 — 카지노 브라우저 런타임 복구

## 사용자 변경 사항

- 베팅 후 카지노 화면 전체가 오류 화면으로 전환되던 문제를 수정했습니다. 클라이언트 초기 상태를 Next.js `use server` 모듈에서 더 이상 내보내지 않습니다.
- 카지노 전용 복구 화면을 추가해 같은 베팅을 연속 제출하기 전에 서버 권위의 최근 게임 기록을 먼저 확인하도록 안내합니다.
- 카지노 로딩 화면을 실제 페이지 구조에 맞춘 전용 스켈레톤으로 개선했습니다.

## 원인

운영 로그에서 참조 코드 `3286936712@E352`가 `frontend/src/app/casino/actions.ts`를 거부한 사실을 확인했습니다. `use server` 파일이 `CASINO_IDLE` 객체와 동기 재내보내기를 런타임 export로 노출했고, Next.js는 이 파일의 런타임 export를 비동기 서버 액션만 허용합니다. 브라우저가 카지노 액션 참조를 로드할 때 이 경계 위반이 발생했습니다.

## 안정성 및 성능

운영 프론트엔드의 `.next/cache/fetch-cache`가 `root` 소유인데 systemd 서비스는 `debian` 사용자로 실행되어 반복적인 `EACCES` 쓰기 실패도 확인했습니다. 현재 Debian 운영 런타임의 캐시 소유권을 바로잡았습니다. 이 운영 조치는 카지노 코드 수정과 별개이며 이후 배포 호스트 위생 점검에 포함되어야 합니다.

## 검증

- 카지노 회귀 테스트 14/14 통과.
- 프론트엔드 TypeScript 통과.
- 저장소 ESLint 통과: 기존 `no-img-element` 경고 11건, 오류 0건.
- Next.js 프로덕션 빌드 통과 및 `/casino` 생성 확인.
- DB 마이그레이션 및 경제 정산 규칙 변경 없음.
