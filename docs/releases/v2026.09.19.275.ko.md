# v2026.09.19.275 — Blue/green 연속성 및 최신 빌드 자동 갱신

## 요약
런타임 배포를 반복 가능한 host blue/green 방식으로 전환하고, 열린 탭이 이전 프론트 빌드이면 로그인 쿠키를 유지한 채 최신 빌드로 자동 이동합니다.

## 변경 사항
- frontend build identity 변경 시 1회 cache-bust 자동 갱신.
- 중간 캐시가 이전 shell을 계속 제공할 때 reload loop를 막고 수동 fallback만 표시.
- stable secret과 release identity를 분리한 Production release 환경 생성기.
- fail-closed Nginx server-block 포트 전환 도구.
- canary health/version → Nginx 전환 → stable systemd 재시작 → stable 검증 → Nginx 복귀 순서의 host blue/green helper.
- primary 복구 실패 시 canary routing을 유지해 의도적 중단을 방지.

## 런타임 승격 전 검증
- stale-tab 프론트 회귀시험 7/7 통과.
- frontend typecheck 통과.
- Nginx 전환 helper 회귀시험 통과.
- Test/Production release-env helper 회귀시험 통과.
- 실 PostgreSQL 인증 세션 연속성 테스트 통과.

## 배포 상태
- 격리 Test: exact-SHA build/승격 대기.
- Production: merged exact-SHA Test 증거 후 승격 대기.
