# v2026.09.19.241 — Server-owned status freshness

## English (canonical)
- Add server-owned freshness metadata to service-status responses.
- Fail stale and materially future observations closed to `unknown`.
- Preserve the existing API fields for backward-compatible clients.

## 한국어
- 서비스 상태 응답에 서버 소유 최신성 메타데이터를 추가했다.
- 오래되었거나 허용 범위를 넘는 미래 관측을 `unknown`으로 처리한다.
- 기존 API 필드는 유지하여 하위 호환성을 보존한다.
