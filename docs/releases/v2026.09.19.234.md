# v2026.09.19.234 — GitHub document-branch reconciliation

Internal/GitHub release note: automatic branch integration now includes `docs/*` branches in exact-SHA validation, conflict reconciliation and terminal failed-branch handling. This fixes the class of stale documentation PRs exemplified by #492 while keeping `planning/*` reserved. No application runtime, database, ledger or DNS behavior is changed.
