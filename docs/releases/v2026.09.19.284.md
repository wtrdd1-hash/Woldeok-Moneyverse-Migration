# v2026.09.19.284 — Sign out all devices

## English canonical
- Adds a user-facing **Sign out on all devices** action to Account Security.
- Requires the existing recent reauthentication gate, revokes every other active session first, then revokes the current session and relays the clearing cookie.
- Reuses the existing authoritative session APIs; no schema or economic mutation is added.

## 한국어
- 계정 보안 센터에 **모든 기기에서 로그아웃** 기능을 추가합니다.
- 기존 최근 본인 확인 gate를 통과한 뒤 다른 활성 세션을 모두 폐기하고 현재 세션도 로그아웃하며 삭제 쿠키를 브라우저에 전달합니다.
- 기존 권위 세션 API를 재사용하며 DB 스키마·경제 상태 변경은 없습니다.
