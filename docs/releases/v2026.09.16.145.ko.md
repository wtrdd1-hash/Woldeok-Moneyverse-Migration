# v2026.09.16.145 — 카지노 테마 액션 계약 수정

## 사용자 영향

럭키 777 슬롯, 하이/로우, 컬러 휠, 보물 상자, 럭키 젬 화면이 카지노 서버 액션이 실제로 읽는 `choice` 필드로 선택값을 전송하도록 수정했습니다. 기존에는 `number` 또는 `parity` 필드로 전송해 정상 선택도 백엔드 정산 API 호출 전에 거절될 수 있었습니다.

## 무결성 및 최적화 점검

정산 구조는 계속 서버 권위 방식입니다. 브라우저는 선택값과 정수 WLD 베팅액만 보내며 RNG, 정책/한도 검사, 멱등성, 원장 정산은 백엔드/DB가 담당합니다. 카지노 페이지의 독립 조회는 이미 병렬 로딩되고 있으며 슬롯의 `Math.random()`은 대기 중 시각 효과에만 사용되고 최종 릴은 서버 영수증 결과로 결정됩니다.

## 검증

공유 계약 빌드 성공, 카지노 프론트 집중 테스트 40/40 성공, 프론트 타입체크 성공, 변경 파일 ESLint 성공을 확인했습니다. 최초 광범위 테스트에서는 작업공간의 `@moneyverse/contract` 미빌드 상태만 확인되었고 계약 빌드 후 집중 검증은 통과했습니다. 실제 승격은 저장소의 Test/Production 게이트를 거쳐야 합니다.
