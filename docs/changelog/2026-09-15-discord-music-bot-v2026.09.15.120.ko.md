# 디스코드 음악 봇 — v2026.09.15.120

## 요약
최신 `main` 위로 음악 기능 후보를 재정렬하고, 음성 채널 `1536572442422550538` 상주 정책을 검증했습니다. 기존 경제/관리자 글로벌 명령어는 삭제하지 않고 길드 전용 음악 명령어만 초기화 후 재등록했습니다.

## 변경 사항
- 부팅 시 자동 입장, `VoiceStateUpdate` 즉시 재진입, 연결 상태 복구, 30초 Watchdog을 유지합니다.
- 길드 전용 음악 명령어를 초기화하고 `/재생`, `/스킵`, `/정지`, `/일시정지`, `/계속재생`, `/대기열`, `/현재곡`을 재등록했습니다.
- 봇의 백엔드 상태 확인 주소를 폐기된 `/api/version`에서 실제 `/health`로 수정했습니다.
- 목표 음성 채널은 `DISCORD_VOICE_CHANNEL_ID`로 설정 가능하며 기본값은 `1536572442422550538`입니다.

## 검증
- 격리 후보 `npm ci`: 통과, 취약점 0건.
- `npm test`, `node --check bot/index.js`, `node --check bot/music.js`: 통과.
- 음악 모듈 스모크: 7개 명령 로드 및 YouTube 허용 URL 검사 통과.
- 운영 백엔드 `/health`: HTTP 200.
- 실행 중인 봇 서비스가 음성 채널 `1536572442422550538`에 연결된 것을 확인했습니다.

## 배포 순서
기능 브랜치 -> 격리 테스트 런타임 -> 백엔드/봇 상태 확인 -> `main` -> 운영 봇 재시작 -> 음성/음악 배포 후 스모크 테스트.
