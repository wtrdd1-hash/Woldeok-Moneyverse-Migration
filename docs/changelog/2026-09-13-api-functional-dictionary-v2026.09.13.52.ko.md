# v2026.09.13.52 — 모바일 API 기능 사전 보강

- 전체 144개 사용자 앱 API 표에 `기능` 열을 추가했다.
- 각 경로가 앱에서 실제로 담당하는 화면/행동을 사람말로 명시했다.
- `인증/CSRF` 열을 추가해 공개, prelogin/OAuth, 로그인 read, 로그인 write를 구분했다.
- `auth/consent`는 회원가입 전 prelogin에서도 사용하는 실제 상태머신에 맞춰 별도 표기했다.
- 통합 API 문서 하나만 읽어도 경로와 제품 기능을 연결할 수 있도록 했다.
