# v2026.09.15.96 — 상점 검색, 직업 제한 실강제, HTTPS 보강

## 요약

- 상점 상품/회원 카탈로그/공개 카탈로그 API에 선택형 `q` 검색을 추가했습니다.
- 웹 상점도 같은 서버 검색 계약을 사용하는 검색창을 추가했습니다.
- 로그인 사용자가 이미 보유한 사업 유형은 신규 사업 구매 카탈로그에서 제외합니다.
- 관리자 제어판에 `work` 기능 스위치를 등록하고 직업/작업 쓰기 API에서 실제로 강제합니다.
- `GET /api/v1/work/tasks`가 현재 `featureState`를 추가로 반환해 앱도 관리자 제한을 동일하게 반영할 수 있습니다.
- 기존 HSTS/CSP/보안 쿠키/내부 loopback 백엔드 구조를 유지하면서 운영 HTTP 요청을 HTTPS로 308 강제 전환합니다.

## 호환성

- `GET /api/v1/work/profile` 기존 응답 형식은 유지합니다.
- 작업 목록의 `featureState`는 기존 응답에 추가되는 필드라 기존 웹 소비자와 호환됩니다.
- 상점 `q`는 선택 사항이며 검색어가 없으면 기존 전체 카탈로그 동작을 유지합니다.

## 직업 기능 스위치 의미

- `enabled`: 신규 및 진행 중 작업 허용
- `paused` / `safe_mode`: 신규 전직·신규 작업·즉시 완료는 차단, 이미 진행 중인 작업은 마무리 가능
- `disabled`: 직업 작업 쓰기 동작 차단

## 배포 게이트

CI 성공 후에만 병합하며, 병합된 main의 정확한 SHA가 격리 Test의 백엔드/DB 검증을 통과한 뒤 운영으로 승격합니다.