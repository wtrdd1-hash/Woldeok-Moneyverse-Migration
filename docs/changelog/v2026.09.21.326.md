# v2026.09.21.326

## English canonical

- Fix off-host DR restore verification so the checksum sidecar cannot redirect verification to an absolute source-host path.
- Validate the sidecar digest format and compare it directly with the SHA-256 of the fetched encrypted archive before decryption.
- Preserve local restore compatibility and all existing isolated PostgreSQL, migration-authority, and ledger-integrity gates.

## 한국어

- off-host DR 복구에서 checksum sidecar의 원본 절대경로를 따라가지 않고 실제로 내려받은 암호화 아카이브의 SHA-256을 직접 검증하도록 수정했다.
- checksum 형식을 검증하고 복호화 전에 fetched archive와 직접 비교한다.
- 로컬 복구 호환성과 기존 격리 PostgreSQL, migration authority, ledger integrity gate는 유지한다.
