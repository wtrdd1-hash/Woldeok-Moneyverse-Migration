# v2026.09.19.289 — 주식 자동 뉴스 런타임 설정

- 무인 AI 주식 시나리오용 배포 소유 OpenAI-compatible 모델 설정을 추가했다.
- 자동 생성은 PostgreSQL의 사람 뉴스룸 API 키 없이 로컬 Ollama를 사용할 수 있다.
- 모든 DB 조회·저장·게시에는 감사 가능한 operator actor가 계속 필수다.
- HTTP(S)가 아닌 런타임 endpoint는 fail-closed 처리한다.
- v285의 실제 등록 주식 전용 제약은 그대로 유지한다.
