# v2026.09.23.399 — 현재 구현 기능 안내 페이지 동기화

- 공개 `/guide` 페이지를 현재 웹/백엔드 코드에 존재하는 사용자 기능 기준으로 갱신했습니다.
- “현재 구현된 주요 기능” 섹션을 추가해 핵심 경제, 시장, 커뮤니티, 세계/콘텐츠, 계정/안전 기능을 설명하며 API 내부 정보는 공개하지 않습니다.
- 오래된 “5개 직업” 문구를 현재 8대 직업 기준으로 수정했습니다.
- 직업의 구형 에너지/쿨다운 설명을 실제 서버의 최소 수행 시간 및 작업별 일일 배정 한도로 교체했습니다.
- 구현 기능 그룹과 연결 경로 유효성을 검증하는 가이드 회귀 테스트를 추가했습니다.
- Test 검증 중 기존 `spaces-view.tsx` 조건부 렌더링 구문 오류와 `CollectionModule`의 `SessionGuard` DI 실패를 발견했습니다. 두 수정은 별도로 `main`에 반영되었으며, 본 브랜치를 해당 최신 `main` 위로 재배치해 중복 수정을 제거했습니다.
