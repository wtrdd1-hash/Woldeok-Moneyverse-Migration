# v2026.09.14.70 — 이메일 인증 전달 안정화

- 회원가입 시 확실성이 높은 이메일 제공자 도메인 오타를 인증메일 발송 전에 차단하고 올바른 주소를 제안합니다.
- SMTP 장애 원인은 추적할 수 있게 하되 전체 이메일 주소, 인증 토큰, 자격증명은 로그에 기록하지 않습니다.
- `SMTP_RETURN_PATH`를 표시용 발신자와 별도로 지정해 반송 알림을 전용 메일함으로 보낼 수 있습니다.
- 운영에서 인증메일 전달 경로가 비정상이면 계속 fail-closed로 처리합니다.
