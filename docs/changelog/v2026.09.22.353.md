# v2026.09.22.353

- Consolidated all six backend ESLint blockers from the current main into one exact-SHA candidate.
- Replaced the TreasuryRepository `any` pool cast with the concrete `pg.Pool` contract.
- Removed five unused backend imports without changing API, DB, migration, privilege, or ledger behavior.
