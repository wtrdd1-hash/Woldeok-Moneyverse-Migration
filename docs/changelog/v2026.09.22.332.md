# v2026.09.22.332

- P1 security: require recent reauthentication before an administrator can restrict or unrestrict a member.
- Preserve the existing admin-session, CSRF, database role check, request-context audit, and API payload/response contract.
- Add a controller metadata regression test so the step-up boundary cannot silently disappear.
