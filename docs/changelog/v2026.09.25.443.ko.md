# v2026.09.25.443 — 데이터베이스 아키텍처 조사·기획

- 원시 80,000건에서 중복 제거된 Crossref DB 아키텍처 탐색 후보 66,858건으로 근거 범위를 확장했다.
- PostgreSQL 17 기준 상세 데이터베이스 아키텍처 명세를 추가했다.
- schema fingerprint, FK-index coverage, 무중단 schema evolution, transaction retry, ledger reconciliation, DB privilege separation을 P0 gate로 추가했다.
- 측정 기반 index lifecycle, 조건부 partitioning, JSONB 경계, 명시적 삭제 semantics, DB maintenance SLO, read-model 분리를 P1 계약으로 추가했다.
- 기존 SQL migration 권위를 유지하고 corpus 규모만으로 ORM/sharding/전면 partitioning을 도입하지 않도록 명시했다.
- 기획/문서 전용이며 runtime DB 변경이나 환경 승격은 없다.
