# v2026.09.19.289 — Automatic stock-news runtime configuration

- Added deployment-owned OpenAI-compatible model settings for unattended AI stock scenarios.
- Automatic generation can now use local Ollama without requiring a human newsroom API key in PostgreSQL.
- The audited operator actor remains mandatory for every database read/write and publication.
- Non-HTTP(S) runtime endpoints fail closed.
- Registered-stock-only constraints from v285 remain authoritative.
