# AI 뉴스 호출자 소유 멱등성 — v2026.09.23.407

- AI 뉴스 설정, 수동 생성, 시나리오 발행·폐기는 HTTP DTO 경계에서 호출자가 만든 UUID 재시도 키를 필수로 요구합니다.
- 외부 명령에 대해 AiNewsService가 재시도 식별자를 임의 생성하던 fallback을 제거했습니다. 기존 프론트 액션은 이미 UUID 키를 전달합니다.
- 내부 자동 생성 명령의 내부 식별자는 유지합니다. migration, ledger, 권한, 인증 의미는 변경하지 않았습니다.
