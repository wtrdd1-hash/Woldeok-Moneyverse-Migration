# Stock comparison share links — v2026.09.14.79

- Rebased the active stock-detail, stock-tagged discussion, and comparison deep-link runtime slices onto the latest `main`.
- Keep `/stocks/compare` synchronized with the selected 2–3 virtual-stock symbols.
- Added a member-facing copy-link action so the exact comparison can be reopened or shared without rebuilding the selection.
- Preserved authoritative integer-string market values and existing server/API contracts.
