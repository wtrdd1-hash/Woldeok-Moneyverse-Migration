# v2026.09.21.324

## English canonical

- Add fail-closed restore-drill support for encrypted backups fetched directly from an `rclone` off-host remote, preserving checksum verification, isolated PostgreSQL restore, migration authority, and ledger-integrity checks.

## 한국어

- `rclone` off-host 원격지의 암호화 백업을 직접 가져와 checksum, 격리 PostgreSQL 복구, migration authority, ledger integrity를 검증하는 fail-closed 복구 훈련 경로를 추가했다.
