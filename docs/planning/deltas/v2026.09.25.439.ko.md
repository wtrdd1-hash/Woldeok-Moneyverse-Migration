# 기획 델타 v2026.09.25.439 — 로그인/세션 영속성 하드 게이트

**상태:** PLANNING  
**날짜:** 2026-09-25  
**시작/중간 origin/main:** `118a58ddc289839957caacd14650863e614f8fad`

## 결정
- P0: 유효한 로그인 세션은 배포, 서비스/호스트 재시작, 프록시 reload, cutover, rollback, 보안 폐기 없는 key rotation을 지나도 유지한다.
- 런타임 교체 자체는 로그아웃 사유가 아니다.
- 허용 로그아웃 사유는 사용자 직접 로그아웃, 명시적 관리자/보안 폐기, compromise 대응, 계정 비활성/삭제, 정상 권위 만료로 제한한다.
- durable/shared session authority와 overlap-safe key validation을 강제한다.
- 배포 전 유효 세션을 guest로 조용히 강등하면 release failure다.
- 동일한 pre-existing authenticated session으로 Test와 Production 연속성 검증을 통과해야 한다.
- 배포 유발 로그아웃 또는 release 기인 auth error spike는 승격 차단/rollback 조건이다.

## 증거 게이트
exact candidate SHA, runtime identity, shared-session health, privacy-safe continuity 식별자, authenticated read와 안전한 CSRF mutation, auth-error 변화가 필요하다. session count 동일성만으로는 부족하다.

## 범위
기획/문서 전용이며 runtime, Test, Production 배포 완료를 주장하지 않는다.
