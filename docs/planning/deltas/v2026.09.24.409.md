# API Contract Authority Recovery and Implementation Reprioritization — v2026.09.24.409

**Status:** PLANNING  
**Authority:** PROJECT_PLAN v2026.09.24.409  
**Start/mid main SHA:** `b8bfc22ddff09f5ccd7632024deac447ff159ff6`

## 1. Observed API planning defects

### G409-01 / P0 — exact-SHA API acceptance not proven
The latest-main planning audit cannot claim a passing API contract gate. The immediately preceding required runtime-check failed on ESLint before API contract verification completed.

**Acceptance:** all required CI checks green on the exact candidate, including `pnpm api:contract:check`, and generated artifacts must match committed artifacts.

### G409-02 / P0 — incompatible inventory counts
Current materials contain:
- machine-readable mobile contract: 179 endpoints;
- complete mobile spec: 57 backend controllers / 335 backend endpoints / 179 mobile endpoints;
- endpoint catalog: 139 mobile endpoints;
- v402 source scan: 58 controller files / 361 HTTP decorators.

These values use different scopes or stale baselines and MUST NOT be combined into one authoritative count.

**Acceptance:** generate a normalized tuple per route: `scope, method, path, operationId, controller, authClass, exposureClass`; derive counts from those tuples.

### G409-03 / P1 — documentation drift
The complete spec, endpoint catalog, machine contract, schema reference and runtime contract do not share one enforced version/count source.

**Acceptance:** generation/check pipeline updates or validates all maintained API documents together. Stale count/version strings must fail CI.

### G409-04 / P0 — high-risk mutation verification order
Financial/economic state changes carry the highest correctness and abuse risk.

**Tier-1 mutation families:** wallet transfers; bank movements/loans/bonds; stock orders; work/reward claims; marketplace purchase/tax/item-sink; casino play/self-limits; treasury/admin economy mutations.

**Acceptance for each mutation:** authn/authz; object-level access; CSRF where browser applicable; recent reauth where sensitive; DTO bounds; idempotency; concurrency; atomic ledger/state transition; stable error model; negative tests; audit evidence.

### G409-05 / P0 — client/API parity
Every web/mobile server-backed action must map to a live API method/path.

**Acceptance:** no dead buttons, client-side fake success, silent mock fallback, 404 target, or native app direct private-backend access.

## 2. Reassigned implementation order

| Order | Stage | Required exit evidence |
|---:|---|---|
| 0 | Restore required CI gates | classify/policy/runtime-check green |
| 1 | Generate exact-SHA API contract | generated method+path+operationId inventory |
| 2 | Reconcile API docs/inventory | one scoped count/version source |
| 3 | Common API security controls | authn/authz/CSRF/reauth/error policy tests |
| 4 | High-risk money/ledger mutations | idempotency/concurrency/atomicity + negative tests |
| 5 | Stocks/realtime/chat reliability | sequence recovery, cursor/idempotency, reconnect tests |
| 6 | Mobile/web BFF parity | route mapping + client E2E, no private backend calls |
| 7 | Admin/control-plane API | role/reauth/audit/destructive-action tests |
| 8 | Automated drift prevention | CI fails on stale contract/docs |
| 9 | Exact-SHA Test E2E | representative reads/writes across domains |
| 10 | Production promotion | zero-downtime, session continuity, post-cutover verification |

## 3. Release rule

Any open blocker in stages 0–9 blocks Production promotion. Matching endpoint counts alone never satisfies API acceptance.

## 4. Evidence boundary

This planning delta does not claim runtime implementation, Test verification, or Production deployment.
