# v2026.09.15.128 — 통합 기획 증거 델타

> 기준 애플리케이션 `main`: `104573a8faec6e622bdc36f65f109ba39896db7c`(PR #343 / 런타임 릴리스 v127). 기획/문서 전용이며 런타임을 배포하지 않는다. 권위 `PROJECT_PLAN.md`/`.ko.md`는 아직 v110이므로 손실 없는 병합 전에는 통합 완료로 표시하지 않는다.

## 1. 외부 레퍼런스 조사·판정

- Google Search Central canonical 최신 문서: **직접채택**. canonical은 강제 명령이 아니라 신호이므로 redirect, sitemap, `rel=canonical`, locale/hreflang, 실제 콘텐츠를 일관되게 유지한다. 검색/필터 query variant를 무제한 색인 URL로 만들지 않는다.
- Google structured data 문서(2026-09-08 업데이트): **allowlist 방식 채택**. 실제 콘텐츠와 일치하고 현재 지원되는 타입만 출력한다. Product markup은 실제 단일 상품/variant 공개 페이지에만 적용하고 카지노/도박 관련 상품에는 Product rich-result markup을 사용하지 않는다. beta carousel은 출시 의존성이 아니다.
- OWASP ASVS 5.0/API Security: **직접채택**. 권한검사, 세션/재인증, BOLA/BFLA 음성 테스트, 자원사용 상한, 자동화/민감 비즈니스흐름 방어, 감사가능성, 경제·관리자 경로 fail-closed를 릴리스 게이트로 유지한다.
- Google Play 서비스 수수료 최신 문서: **변수형 입력으로 채택**. 시장/설치 cohort/반복·비반복/프로그램/billing 경로에 따라 수수료가 다르므로 unit economics에 전역 15%/30% 상수를 두지 않는다.

## 2. 최신 main / QA / CI 증거

- PR #343이 `104573a8...`에 병합됐다. PR 기재 QA는 typecheck PASS, API contract 147 endpoints PASS, production build PASS, frontend 601 tests PASS, fresh PostgreSQL migration 001→198 PASS, backend fresh-DB 1097 pass / 323 conditional skip / 0 fail이다.
- migration 198은 카지노 platform daily stake/loss headroom을 약 `9e18`로 올리면서 per-play bound와 회원 소유 self-limit/self-exclusion은 유지한다. work quota 및 casino refusal에 안정적인 machine-readable HTTP 409 code가 추가됐다.
- 이 exact SHA의 최신 관측 `Build Production Release` #764는 `completed/skipped`다. 따라서 Production-ready의 긍정 증거가 아니다. runtime exact-SHA, Production smoke, restore 증거는 각각 별도로 필요하다.

## 3. ECON-128-01 — P0 — REDESIGN_REQUIRED — 카지노 platform headroom 사실상 무제한

**심각도/증거:** P0 경제악용·신뢰 위험. 2026-09-15 PR #343에서 최초 확인했고 현재 main의 code/migration으로 재확인했다.

**영향 흐름:** 로그인 회원 → casino terms → coin/dice 반복 플레이 → WLD debit/credit → casino scoped balance/ledger → 일일 counter. per-play max는 200이지만 platform 일일 stake/loss ceiling이 약 9e18이어서 실질적인 platform 일일 guard가 아니다.

**위험:** WLD는 simulation-only이므로 실도박 매출을 의미하지 않지만, 자동화 고빈도 플레이, 계정 탈취, multi-accounting, replay/idempotency 결함, reward/casino arbitrage 또는 향후 balance 정책 변경 시 극단적인 가상 손실과 ledger volume이 누적될 수 있다. 명목상 daily limit가 실제 안전장치처럼 보이면 신뢰·CS 위험도 생긴다.

**구체 수정설계:** 회원 self-limit/self-exclusion은 엄격한 사용자 안전통제로 유지한다. 별도의 서버 권위 `casino_platform_risk_policy`에 유한한 `daily_stake_cap`, `daily_loss_cap`, `plays_per_minute`, `plays_per_day`, 선택적 account-age/risk tier, effective-from/version, emergency-disable을 둔다. UI는 `회당 한도`, `회원 self-limit`, `platform risk cap`을 구분하며 9e18을 의미 있는 사용자 allowance로 표시하지 않는다. 클라이언트는 정책을 올릴 수 없다.

**API:** `GET /casino/terms`는 canonical decimal string과 machine code를 반환한다. mutation은 self-limit/platform-limit/insufficient-funds/idempotency conflict를 안정적 409로, rate/automation 제한은 429로 반환한다. transport validation은 400, 인증/권한은 401/403으로 분리한다. 가치변경 재시도는 actor+operation+payload hash에 묶인 idempotency key가 필요하다.

**DB/트랜잭션:** policy/counter 읽기, eligibility, debit/credit ledger, casino balance, daily counter, idempotency, audit/outbox를 원자적으로 commit한다. row/advisory lock 또는 동등한 직렬화로 동시 플레이가 cap을 넘지 못하게 한다. counter 음수/overflow-invalid 상태를 constraint로 거절한다. migration 198은 불변이며 교정은 199+로 한다.

**보안/악용:** BOLA/BFLA, replay, concurrent duplicate play, multi-account burst, bot loop, oversized numeric input, outcome/stake tamper, stale terms, 날짜경계 악용, admin policy mutation을 음성 테스트한다. `casino_plays_per_actor_minute`, cap rejection, duplicate idempotency, 비정상 net issuance/sink, ledger rows/play를 관측한다. device/IP 상관분석은 개인정보·법적 정책이 허용할 때만 사용하고 secret/session ID는 로그에 남기지 않는다.

**UX/접근성:** empty/loading/error/offline/timeout에서 성공으로 오인시키지 않는다. mutation 미확정 동안 중복 CTA를 막고 timeout 후 idempotency/status로 reconciliation한 뒤 다음 가치변경을 허용한다. 스크린리더는 stake, 가능한 simulation 결과, 거절 이유, 갱신 WLD를 색상 외 수단으로 읽을 수 있어야 한다. 모바일/태블릿/데스크톱은 같은 서버 정책을 사용한다.

**QA:** cap−1/cap/cap+1 단위테스트, 실DB concurrent play, 날짜/시간대 경계, network timeout retry, duplicate idempotency, self-exclusion, session revoke, 409 localization, 429 backoff, 복식부기 reconciliation, fresh/upgrade migration, rollback compatibility를 검증한다.

**Test/운영 승격조건:** exact-SHA Test에서 유한 정책, concurrency cap 원자성, duplicate value 0, UI 오표시 0, casino ledger reconciliation을 증명한다. near-unbounded 정책은 명시적으로 승인된 실험·위험기록이 없으면 Production 승격을 차단한다.

**사업성 가설:** 카지노는 실화폐 매출이 아니라 retention/economy sink다. sessions/user, D1/D7/D30, self-limit adoption, casino WLD sink/source ratio, support contacts, 손실 후 churn, abuse incident, infra cost/1k plays를 본다. retention 개선과 guardrail 안정이 동시에 있을 때만 SCALE, engagement 상승과 guardrail 악화 시 ITERATE, ledger duplication/cap bypass/중대한 신뢰훼손 시 KILL/disable한다.

## 4. WORK-128-01 — P1 — PARTIAL/UNVERIFIED — work quota conflict 계약

관리자 설정 work reward quota를 사용자가 이해 가능한 machine-actionable 상태로 만들되 클라이언트를 권위로 만들지 않는다. UI는 read-model로 이미 소진된 action을 선제 disable할 수 있지만 mutation은 동일 권위 transaction에서 quota를 다시 검사한다. 409 code는 locale별 안내와 analytics enum으로 매핑하고 raw backend message를 비즈니스 로직으로 사용하지 않는다. stale UI→quota exhausted, concurrent last-slot, retry/idempotency, 세션 중 admin policy 변경, 401/403/429 분리, reward/ledger 원자성을 테스트한다. KPI는 attempt→success, expected-409, duplicate attempts, support contacts, D1/D7이며 매출로 계산하지 않는다.

## 5. SEO / SEO 백엔드

casino, bank, portfolio, support, account, admin, transaction은 인증 페이지로 `noindex`, sitemap 제외, structured-data 제외다. 공개 페이지는 서버 권위 `SeoDocument` read-model에서 canonical, robots, title/description, locale/hreflang, breadcrumb, OG, image metadata, `updatedAt/lastModified`, structured-data allowlist, redirect-map version을 생성한다. search/filter query는 독자적 고품질 curated 콘텐츠가 없는 한 durable collection URL로 canonicalize한다. Product structured data는 Google 지원요건을 만족하는 실제 공개 상품에만 사용하고 casino/gambling product markup은 금지한다. HTML metadata/sitemap/redirect/canonical cache invalidation은 같은 변경에 일관되게 반영한다. KPI는 impressions→CTR→organic visit→signup→activation→D7/D30→net revenue를 국가/검색표면 cohort로 본다.

## 6. 수익성 / unit economics

실결제 SKU/구독은 `gross_price`, 세금, 시장, install cohort, recurring 여부, Play/Apple/web billing path, platform/billing fee, refund 가정, variable infra/support cost, net revenue, contribution margin을 변수로 둔다. attach rate, ARPU/ARPDAU/ARPPU, churn, refund, CAC, LTV의 미실측값은 `가설/테스트 기준`이다. casino WLD turnover는 revenue로 계상하지 않는다. 광고는 광고매출에서 광고유발 session/retention 감소를 차감한 순효과로 본다. 보안/QA/백업/관측성은 사고·다운타임·환불·부정사용·CS 비용 절감으로 평가한다.

## 7. 우선순위/개발 연결

P0 recovery evidence/release truth/auth+authorization/economy integrity가 신규 수익화보다 우선한다. 이번 델타는 `ECON-128-01 → exact-SHA casino 실DB/보안 QA → release evidence → WORK-128-01 회귀 → SEO/수익화 성장` 순서다. 런타임 구현은 `새 브랜치 → CI/tests → isolated exact-SHA Test → backend/API/DB/user-flow QA → main → exact-main 재검증 → Production 승격 → smoke/monitoring/rollback`을 따른다.

## 8. 권위문서 동기화 상태

`PROJECT_PLAN.md`와 `PROJECT_PLAN.ko.md`는 아직 v110이며 이후 구현과 충돌하는 관리자 TOTP 요구도 포함한다. 현재 connector writer는 대형 파일 전체 replacement 방식이고 읽기 결과가 잘리므로 강제 replacement는 데이터 유실 위험이 있다. 상태는 `DOC-117-01 / P1 / BLOCKED_BY_SAFE_WRITE_CAPABILITY`. 이 델타는 증거이며 권위 통합을 대체하지 않는다.