# v2026.09.25.443 — 데이터베이스 아키텍처 근거·계약 델타

- DB 아키텍처 10개 탐색 lane에서 원시 80,000개 후보를 수집하고 DOI 우선/정규화 제목 보조 중복 제거 후 **66,858건 Crossref 탐색 corpus**를 구성했다. 이는 Tier C 탐색 근거이며 broad-term false positive를 채택 근거로 사용하지 않는다.
- 현재 Moneyverse DB 권위인 numbered SQL migration, immutable checksum, 제한된 runtime role, PostgreSQL 보호 function, exact money 표현, DB-owned atomic/idempotent economy mutation을 핵심 기반으로 재확인했다.
- 상세 권위 `DATABASE_ARCHITECTURE_SPEC`을 추가했다.
- **DB443-01 / P0:** exact-SHA generated schema fingerprint/catalog drift gate.
- **DB443-02 / P0:** canonical table key/constraint audit.
- **DB443-03 / P0:** PostgreSQL이 referencing FK column을 자동 index하지 않으므로 child-side FK index coverage audit.
- **DB443-04 / P0:** lock/rewrite 분류를 포함한 expand/backfill/validate/switch/contract 무중단 migration protocol.
- **DB443-05 / P0:** business idempotency를 유지하는 serialization/deadlock transaction 전체 bounded retry.
- **DB443-06 / P0:** ledger와 balance projection의 reconciliation/rebuild 계약.
- **DB443-07 / P0:** migration owner/runtime role 분리와 security-definer grant/search-path 검증.
- **DB443-08..13 / P1:** query/index evidence, 조건부 partitioning, typed-core/JSONB 경계, FK delete semantics, maintenance SLO, hot-path/read-model 분리.
- **DB443-14 / P0/P1:** 검증된 logical backup을 유지하면서 WAL/PITR 및 immutable off-host recovery는 실제 restore drill로만 수용.
- corpus 규모만으로 ORM migration 전환, 전면 partitioning, generic sharding, PostgreSQL major upgrade를 채택하지 않는다.
- 조사/기획/문서 전용이며 schema migration, Test 배포, Production 승격 완료를 주장하지 않는다.
