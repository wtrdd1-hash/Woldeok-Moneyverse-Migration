import { PublicAdvertisement } from './public-advertisement';

/** The landing page is one of the explicit public-information allowlist entries. */
export function HomeAdvertisement() {
  return <PublicAdvertisement />;
}
